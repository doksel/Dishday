# React 19 is newer than what some libs (Radix UI, react-day-picker, …) declare
# as peer. The combination works at runtime, but npm 10+ blocks the install
# unless we explicitly tell it to fall back to legacy resolution.
# Set globally for this repo so contributors don't have to remember the flag.
legacy-peer-deps=true
